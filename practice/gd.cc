#include<cstdlib>
#include<cstdio>
#include<string.h>
#include<list>
#include<iterator>
using namespace std;

int main() {
    int n;
    FILE* fin = fopen("C-small-2-attempt0.in", "r");
    FILE* fout = fopen("C-small-2-attempt0.out", "w");
    fscanf(fin, "%d\n", &n);

}